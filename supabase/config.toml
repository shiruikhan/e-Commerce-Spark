# Supabase project configuration
# Ref: https://supabase.com/docs/guides/cli/config


[functions.sync-produtos]
verify_jwt = false   # chamada pelo pg_cron (sem JWT de usuário)

[functions.sync-categorias]
verify_jwt = false   # chamada pelo pg_cron (sem JWT de usuário)

[functions.sync-estoque]
verify_jwt = false   # chamada pelo pg_cron (sem JWT de usuário)

[functions.sync-bairros]
verify_jwt = false   # chamada pelo pg_cron (sem JWT de usuário)

[functions.integrar-clientes]
verify_jwt = false   # chamada pelo pg_cron (sem JWT de usuário)

[functions.integrar-pedidos]
verify_jwt = false   # chamada pelo pg_cron (sem JWT de usuário)

[functions.test-sankhya-auth]
verify_jwt = true    # diagnóstico manual — requer token de serviço
